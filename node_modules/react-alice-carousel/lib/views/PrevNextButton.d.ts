/// <reference types="react" />
export declare const PrevNextButton: ({ name, disabled, onClick }: Props) => JSX.Element;
declare type Props = {
    name: 'prev' | 'next';
    disabled: boolean;
    onClick: (e: any) => void;
};
export {};
