import { EasingType } from "../types";
export declare function dot(a1: any, a2: any, b1: number, b2: number): any;
export declare function dotValue(time: number, prevTime: number, nextTime: number, prevValue: any, nextValue: any, easing?: EasingType): any;
